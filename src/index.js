
import { OpenRouter } from "@openrouter/sdk";
const API_VERSION="2026-08-14",MAX_BODY_BYTES=65536,DEFAULT_LEASE_SECONDS=1800,MAX_LEASE_SECONDS=7200,DEFAULT_RATE_PER_MIN=60;
function now(){return new Date().toISOString()} function rid(){return crypto.randomUUID()} function int(v,d){const n=Number(v);return Number.isFinite(n)?Math.trunc(n):d} function json(body,status=200,headers={}){return Response.json(body,{status,headers:{"cache-control":"no-store",...headers}})} function error(code,message,status=400,details){return json({ok:false,error:code,message,details},status)}
async function parseJson(req){const len=Number(req.headers.get("content-length")||0);if(len>MAX_BODY_BYTES)throw Object.assign(new Error("BODY_TOO_LARGE"),{status:413});const t=await req.text();if(new TextEncoder().encode(t).length>MAX_BODY_BYTES)throw Object.assign(new Error("BODY_TOO_LARGE"),{status:413});if(!t)return{};try{return JSON.parse(t)}catch{throw Object.assign(new Error("INVALID_REQUEST"),{status:400})}}
function redact(v){if(Array.isArray(v))return v.map(redact);if(v&&typeof v==="object"){const o={};for(const[k,x]of Object.entries(v))o[k]=/token|secret|password|authorization|cookie|api.?key/i.test(k)?"[REDACTED]":redact(x);return o}return v}
async function sha256Text(text){const hash=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(text));return[...new Uint8Array(hash)].map(x=>x.toString(16).padStart(2,"0")).join("")} async function sourceDigest(service,policy,capabilities){return sha256Text(JSON.stringify({service,api_version:API_VERSION,policy,capabilities}))}
function gate(env){return env.CENTER_GATE.get(env.CENTER_GATE.idFromName("global"))} async function gateCall(env,path,method="GET",body){const init={method,headers:{"content-type":"application/json"}};if(body!==undefined)init.body=JSON.stringify(body);const r=await gate(env).fetch(new Request(`https://gate.internal${path}`,init));const b=await r.json().catch(()=>({ok:false,error:"GATE_BAD_RESPONSE"}));return{http:r.status,...b}} async function acquire(env,taskId,kind,leaseSeconds){return gateCall(env,"/acquire","POST",{task_id:taskId,kind,lease_seconds:leaseSeconds})} async function release(env,taskId){return gateCall(env,"/release","POST",{task_id:taskId})} async function saveTask(env,taskId,patch){return gateCall(env,`/task/${encodeURIComponent(taskId)}`,"POST",patch)} async function loadTask(env,taskId){return gateCall(env,`/task/${encodeURIComponent(taskId)}`)} async function checkRate(env){return gateCall(env,"/rate","POST",{limit:int(env.RATE_LIMIT_PER_MIN,DEFAULT_RATE_PER_MIN)})}
export class CenterGate{constructor(state,env){this.state=state;this.env=env}async fetch(req){const u=new URL(req.url),s=this.state.storage,body=async()=>{try{return await req.json()}catch{return{}}},active=async()=>{const a=await s.get("active");if(a&&a.expires_at_ms<=Date.now()){await s.delete("active");return null}return a||null};if(req.method==="GET"&&u.pathname==="/state")return json({ok:true,active:await active()});if(req.method==="POST"&&u.pathname==="/acquire"){const b=await body(),a=await active();if(a&&a.task_id!==b.task_id)return json({ok:false,error:"BUSY",active:a},409);const lease=Math.max(30,Math.min(MAX_LEASE_SECONDS,int(b.lease_seconds,DEFAULT_LEASE_SECONDS))),rec={task_id:String(b.task_id||""),kind:String(b.kind||"task"),acquired_at:now(),expires_at_ms:Date.now()+lease*1000};if(!rec.task_id)return json({ok:false,error:"INVALID_REQUEST"},400);await s.put("active",rec);return json({ok:true,active:rec})}if(req.method==="POST"&&u.pathname==="/release"){const b=await body(),a=await active();if(!a)return json({ok:true,released:false});if(a.task_id!==b.task_id)return json({ok:false,error:"LOCK_OWNER_MISMATCH",active:a},409);await s.delete("active");return json({ok:true,released:true})}if(req.method==="POST"&&u.pathname==="/rate"){const b=await body(),limit=Math.max(1,Math.min(1000,int(b.limit,DEFAULT_RATE_PER_MIN))),minute=Math.floor(Date.now()/60000),key=`rate:${minute}`,count=(await s.get(key)||0)+1;await s.put(key,count);await s.delete(`rate:${minute-2}`);return count<=limit?json({ok:true,count,limit}):json({ok:false,error:"RATE_LIMITED",count,limit},429)}let m=u.pathname.match(/^\/task\/([^/]+)$/);if(m&&req.method==="GET")return json({ok:true,task:await s.get(`task:${decodeURIComponent(m[1])}`)||null});if(m&&req.method==="POST"){const id=decodeURIComponent(m[1]),b=await body(),old=await s.get(`task:${id}`)||{},rec={...old,...redact(b),task_id:id};await s.put(`task:${id}`,rec);return json({ok:true,task:rec})}m=u.pathname.match(/^\/task\/([^/]+)\/cancel$/);if(m&&req.method==="POST"){const id=decodeURIComponent(m[1]),key=`task:${id}`,old=await s.get(key);if(!old)return json({ok:false,error:"TASK_NOT_FOUND"},404);const rec={...old,cancel_requested:true,cancel_requested_at:now()};await s.put(key,rec);return json({ok:true,task:rec})}return json({ok:false,error:"GATE_ROUTE_NOT_FOUND"},404)}}
const SERVICE="expert-worker";
const POLICY={fail_closed:true,single_active_task:true,network:"openrouter-only",tools:false,web:false,history_weight:0,reasoning_effort:"high",exclude_providers:["openai","anthropic"],max_retries:0,max_models:4,judge_required:true};
const CAPABILITIES={task_profiler:true,dynamic_models:true,company_diversity:true,experts:true,judge:true,openrouter_sdk:true,tools:false,web:false};
const OPENAPI='{"openapi":"3.1.0","info":{"title":"Expert Center","version":"2026-08-14"},"paths":{"/health":{"get":{}},"/v1/policy":{"get":{}},"/v1/capabilities":{"get":{}},"/v1/run":{"post":{}},"/v1/status":{"post":{}},"/v1/cancel":{"post":{}}}}';
function ready(env){return{provider:"openrouter",configured:Boolean(env.OPENROUTER_API_KEY),sdk:"@openrouter/sdk"}} function allowedModel(model){const m=String(model||"").toLowerCase();return m&&!m.startsWith("openai/")&&!m.startsWith("anthropic/")&&!m.includes("claude")}
async function callModel(env,model,messages,maxTokens){const client=new OpenRouter({apiKey:env.OPENROUTER_API_KEY});return client.chat.send({chatRequest:{model,messages,maxTokens:maxTokens||undefined,reasoning:{effort:"high"},temperature:0.2}})}
async function handle(req,env,ctx){const u=new URL(req.url);if(req.method==="GET"&&u.pathname==="/health")return json({ok:true,status:"ready",service:SERVICE,api_version:API_VERSION,source:"github-static-relay",openrouter:ready(env)});if(req.method==="GET"&&(u.pathname==="/v1/policy"||u.pathname==="/policy"))return json({ok:true,service:SERVICE,policy:POLICY});if(req.method==="GET"&&(u.pathname==="/v1/capabilities"||u.pathname==="/capabilities"))return json({ok:true,service:SERVICE,capabilities:CAPABILITIES,openrouter:ready(env)});if(req.method==="GET"&&(u.pathname==="/quota"||u.pathname==="/v1/quota"))return json({ok:true,rate_limit_per_min:int(env.RATE_LIMIT_PER_MIN,20),single_active_task:true,max_models:POLICY.max_models,max_retries:0});if(req.method==="GET"&&u.pathname==="/openapi.json")return new Response(OPENAPI,{headers:{"content-type":"application/json","cache-control":"no-store"}});if(req.method==="GET"&&u.pathname==="/source")return json({ok:true,service:SERVICE,api_version:API_VERSION,source_digest:await sourceDigest(SERVICE,POLICY,CAPABILITIES),secrets_redacted:true});if(req.method==="GET"&&u.pathname==="/v1/acceptance/latest")return json({ok:true,service:SERVICE,status:"not_verified",run_id:null,receipt_digest:null});if(req.method==="POST"&&u.pathname==="/v1/status"){const b=await parseJson(req);if(!b.task_id)return error("INVALID_REQUEST","task_id required",400);const t=await loadTask(env,b.task_id);return t.task?json({ok:true,task:t.task}):error("INVALID_REQUEST","Task not found",404)}if(req.method==="POST"&&u.pathname==="/v1/cancel"){const b=await parseJson(req);if(!b.task_id)return error("INVALID_REQUEST","task_id required",400);const r=await gateCall(env,`/task/${encodeURIComponent(b.task_id)}/cancel`,"POST",{});await release(env,b.task_id);return json({ok:r.ok,task:r.task||null})}if(req.method==="POST"&&u.pathname==="/v1/run"){const rate=await checkRate(env);if(!rate.ok)return error("RATE_LIMITED","Expert budget exceeded",429,rate);const b=await parseJson(req),taskId=String(b.task_id||b.request_id||rid());if(!env.OPENROUTER_API_KEY)return error("UPSTREAM_AUTH_FAILED","OPENROUTER_API_KEY is not configured in Cloudflare",503);const models=(Array.isArray(b.models)?b.models:[]).filter(allowedModel).slice(0,POLICY.max_models);if(models.length===0)return error("INVALID_REQUEST","At least one allowed non-OpenAI/non-Anthropic model is required",400);if(!b.prompt||typeof b.prompt!=="string")return error("INVALID_REQUEST","prompt required",400);const acq=await acquire(env,taskId,"expert",Math.min(900,int(b.timeout_seconds,300)));if(!acq.ok)return error("BUSY","Another expert task is active",409,acq.active);await saveTask(env,taskId,{status:"running",models,created_at:now(),request_id:b.request_id||null});try{const answers=[];for(let i=0;i<models.length;i++){const role=Array.isArray(b.roles)&&b.roles[i]?String(b.roles[i]):`Expert ${i+1}`,messages=[{role:"system",content:`You are ${role}. Analyze independently. Do not use tools or web. Be evidence-sensitive, explicit about uncertainty, and concise.`},{role:"user",content:b.prompt}],out=await callModel(env,models[i],messages,int(b.max_tokens,0)||undefined);answers.push({model:models[i],role,content:out?.choices?.[0]?.message?.content||"",usage:redact(out?.usage||null)})}let judge=null;const judgeModel=allowedModel(b.judge_model)?String(b.judge_model):models[0];if(answers.length){const synthesis=`Question:\n${b.prompt}\n\nIndependent expert answers:\n${answers.map((a,i)=>`[${i+1}] ${a.model} (${a.role})\n${a.content}`).join("\n\n")}`,jout=await callModel(env,judgeModel,[{role:"system",content:"You are the final judge. Compare the independent analyses, resolve conflicts, identify unsupported claims, and return the best calibrated synthesis. Do not use tools or web."},{role:"user",content:synthesis}],int(b.max_tokens,0)||undefined);judge={model:judgeModel,content:jout?.choices?.[0]?.message?.content||"",usage:redact(jout?.usage||null)}}const rec={status:"completed",models,answers,judge,finished_at:now()};await saveTask(env,taskId,rec);await release(env,taskId);return json({ok:true,task_id:taskId,...rec})}catch(e){await saveTask(env,taskId,{status:"failed",error:"UPSTREAM_UNAVAILABLE",message:String(e?.message||e),finished_at:now()});await release(env,taskId);return error("UPSTREAM_UNAVAILABLE","OpenRouter expert execution failed",502,{task_id:taskId})}}return error("INVALID_REQUEST","Route not found",404)}
export default{async fetch(req,env,ctx){try{return await handle(req,env,ctx)}catch(e){return error(e.message||"INTERNAL_ERROR","Request failed",e.status||500)}}};
