const RUN="medmcqa-gold-mirror-v2-20260817";
const DATASET="aistairc/medLLM_QA_benchmark:data/en/MedMCQA/medmcqa.jsonl";
const EXPECTED_BLOB_SHA="91205dc035b83fd173464aa46e0008302a0b3771";
const CHUNK=23;
const MAX_DATASET_BYTES=2_000_000;
const json=(x,s=200)=>Response.json(x,{status:s,headers:{"cache-control":"no-store"}});
async function gitBlobSha(bytes){const head=new TextEncoder().encode(`blob ${bytes.byteLength}\0`),all=new Uint8Array(head.byteLength+bytes.byteLength);all.set(head);all.set(bytes,head.byteLength);const h=await crypto.subtle.digest("SHA-1",all);return[...new Uint8Array(h)].map(x=>x.toString(16).padStart(2,"0")).join("")}
async function uploadedDataset(req){const declared=Number(req.headers.get("content-length")||0);if(declared>MAX_DATASET_BYTES)throw Object.assign(new Error("DATASET_TOO_LARGE"),{status:413});const bytes=new Uint8Array(await req.arrayBuffer());if(bytes.byteLength>MAX_DATASET_BYTES)throw Object.assign(new Error("DATASET_TOO_LARGE"),{status:413});const sha=await gitBlobSha(bytes);if(sha!==EXPECTED_BLOB_SHA)throw Object.assign(new Error("DATASET_BLOB_SHA_MISMATCH"),{status:400});const raw=new TextDecoder().decode(bytes),lines=raw.split(/\r?\n/).filter(Boolean);let first=null,last=null;try{first=JSON.parse(lines[0]);last=JSON.parse(lines.at(-1))}catch{throw Object.assign(new Error("DATASET_BAD_JSONL"),{status:400})}for(const row of [first,last]){if(typeof row?.problem_text!=="string"||!Array.isArray(row?.choices)||row.choices.length!==4||!/^[abcd]$/i.test(String(row?.answer?.[0]||"")))throw Object.assign(new Error("DATASET_SCHEMA_MISMATCH"),{status:400})}return{bytes:bytes.byteLength,blob_sha:sha,total:lines.length,first_id:String(first.problem_id||""),last_id:String(last.problem_id||"")}}
async function probeUpload(req){try{const d=await uploadedDataset(req);return json({ok:true,run:RUN,dataset:DATASET,upload_verified:true,chunk_size:CHUNK,...d})}catch(e){return json({ok:false,error:String(e?.message||e),upload_verified:false},e?.status||500)}}
export async function benchmarkMedMcqa(req){const u=new URL(req.url);if(req.method==="POST"&&u.pathname.endsWith("/probe-upload"))return probeUpload(req);return json({ok:false,error:"NOT_FOUND"},404)}
export const BENCHMARK_META={run:RUN,chunk:CHUNK,dataset:DATASET,expected_blob_sha:EXPECTED_BLOB_SHA};
