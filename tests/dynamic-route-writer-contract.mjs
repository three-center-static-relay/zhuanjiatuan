import assert from "node:assert/strict";
import fs from "node:fs";
const source=fs.readFileSync(new URL("../scripts/apply-adaptive-expert-route-v3.mjs",import.meta.url),"utf8");
for(const s of ["intelligence-high-to-low","latency-low-to-high","throughput-high-to-low","context-high-to-low","pricing-low-to-high","top-weekly"])assert.match(source,new RegExp(s));
assert.match(source,/supported_parameters=reasoning/);
assert.match(source,/output_modalities=text/);
assert.match(source,/bannedCompanies/);
assert.match(source,/anthropic/);
assert.match(source,/claude/);
assert.match(source,/flash/);
assert.match(source,/freeModel/);
assert.match(source,/openrouter\/free/);
assert.match(source,/MAX_LANES=8/);
assert.match(source,/metadata\.lane/);
assert.match(source,/metadata\.stage/);
assert.match(source,/metadata\.capability/);
assert.match(source,/metadata\.depth/);
assert.match(source,/metadata\.cost_mode/);
assert.match(source,/provider:"openrouter"/);
assert.match(source,/retries:0/);
assert.match(source,/type:"percentage"/);
assert.match(source,/cloudflareTelemetry/);
assert.match(source,/success_rate/);
assert.match(source,/avg_duration/);
assert.match(source,/avg_cost/);
assert.match(source,/addCapabilityChain/);
assert.match(source,/metadata\.capability.*\$eq/s);
assert.match(source,/CLOUDFLARE_AI_GATEWAY_API_TOKEN/);
assert.match(source,/\/logs\?per_page=/);
assert.match(source,/\/ai-gateway\/gateways\//);
assert.match(source,/\/versions/);
assert.match(source,/\/deployments/);
assert.match(source,/--dry-run/);
assert.match(source,/--no-deploy/);
assert.doesNotMatch(source,/type:"rate"/);
assert.doesNotMatch(source,/CLOUDFLARE_BUILDS_API_TOKEN/);
assert.doesNotMatch(source,/console\.(log|error)\([^\n]*API_TOKEN/);
console.log(JSON.stringify({ok:true,suite:"dynamic-route-writer-contract",one_command_writer:true,multi_signal:true,cloudflare_feedback:true,capability_specific:true,free_models:true,eight_company_lanes:true,canary_percentage:true,secrets_redacted:true}));
